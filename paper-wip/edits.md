Should-fix (precision)
1. "TDLAS relies on multipass Herriott cells" (§1). Overstated — TDLAS uses them but doesn't depend on them; the full paper says "often employs." One-word fix, no length change.
Before: "tunable diode laser absorption spectroscopy (TDLAS)³ relies on multipass Herriott cells to fold a long optical path"
After: "tunable diode laser absorption spectroscopy (TDLAS)³ often uses multipass Herriott cells to fold a long optical path"
2. "intracavity reflection pattern" (Abstract and §2). The camera images the spot pattern on the output mirror from outside; "intracavity" implies the sensor is inside the cavity. This is the one phrase that recurs, so fix both.
Abstract before: "directly from the intracavity reflection pattern."
Abstract after: "directly from the cavity's reflection pattern."
§2 before: "a camera observes the pattern (Figure 1)"
§2 after: leave as is — §2 is already fine; only the abstract uses "intracavity." (So this is a single one-word deletion in the abstract, not two edits. I over-counted last time.)
3. Figure 3 caption vs. axis labels. Caption says "induced misalignment (axes)"; axes read "X origin (°)" / "Y origin (°)." Same idea, two names. Cheapest fix is the caption (no figure regeneration):
Before: "Correction steps to realign (color) vs. induced misalignment (axes)."
After: "Correction steps to realign (color) vs. starting origin (axes)."
(If you'd rather keep "induced misalignment" for the recovery framing, relabel the plot axes instead — but the caption edit is zero-effort.)
Optional (only if you have a spare line)
4. Softening around refs [1,2]. You're keeping them, which is fine — but the sentence "Low-cost, field-deployable optical instrumentation is increasingly adopted for industrial and energy systems [1,2]" claims broad adoption, whereas [1,2] are two specific FBG-sensor papers. It reads as a self-cite stretched to cover a general claim. Length-neutral tightening that makes the citation fit what it supports:
Before: "Low-cost, field-deployable optical instrumentation is increasingly adopted for industrial and energy systems.¹﻿﻿′﻿²"
After: "Fiber-optic and free-space optical sensors are increasingly deployed in industrial and energy systems.¹﻿﻿′﻿²"
This narrows "optical instrumentation … adopted" to "optical sensors … deployed," which [1,2] actually exemplify. Keeps both refs, same length.
